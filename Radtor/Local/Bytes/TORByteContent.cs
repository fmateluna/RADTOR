using System;
using System.IO;

namespace IndexByte
{
    public class TORByteContent
    {
        public string torzip { get; set; }
        public bool create()
        {
            BinaryWriter Writer = null;

            try
            {

                Writer = new BinaryWriter(File.OpenWrite(torzip));

                foreach (byte bit in TORByteContentBytesSub1.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub2.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub3.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub4.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub5.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub6.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub7.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub8.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub9.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub10.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub11.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub12.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub13.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub14.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub15.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub16.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub17.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub18.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub19.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub20.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub21.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub22.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub23.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub24.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub25.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub26.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub27.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub28.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub29.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub30.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub31.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub32.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub33.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub34.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub35.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub36.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub37.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub38.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub39.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub40.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub41.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub42.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub43.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub44.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub45.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub46.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub47.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub48.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub49.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub50.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub51.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub52.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub53.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub54.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub55.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub56.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub57.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub58.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub59.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub60.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub61.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub62.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub63.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub64.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub65.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub66.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub67.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub68.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub69.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub70.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub71.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub72.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub73.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub74.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub75.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub76.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub77.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub78.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub79.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub80.DATA) { Writer.Write(bit); }

                foreach (byte bit in TORByteContentBytesSub81.DATA) { Writer.Write(bit); }

                Random rnd = new Random();
                Byte[] b = new Byte[322];
                rnd.NextBytes(b);
                for (int i = 0; i <= b.GetUpperBound(0); i++)
                    Writer.Write(b[i]);

                Writer.Flush();
                Writer.Close();
            }
            catch
            {
                return false;
            }
            return true;
        }
    }
}
